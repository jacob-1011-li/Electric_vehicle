version: 2
 
models:

    - name: dim_charging_station
      description: "Start point of charging station dim table"
      columns:
          - name: station_id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null